# Governed Spatial Render Revision 7 Clean Independent Re-review Handoff

Date: 2026-07-11 (Europe/Vienna)
Status: controller-issued, fresh clean independent review required
Required model: `gpt-5.6-sol`
Decision ceiling: exactly `ACCEPT` or `REVISE` for design canon only
Runtime implementation authorized: no
Provider, quota, build, canary, deployment, publication, promotion, or readiness authority: none

## Mission

Perform one clean, fresh, independent adversarial review of the exact Revision 6 governed spatial-render canon. Revision 7 changes no canonical design byte. It exists only to obtain a complete independent decision after the Revision 6 reviewer returned `REVISE` for process-integrity violations and incomplete independent matrix coverage.

The new reviewer must independently author and execute the complete no-file adversarial matrix. It must not trust, replay, transform, or inspect any controller or prior-reviewer harness command or implementation. Controller counts, prior reviewer spot checks, and packet claims are evidence to challenge, not evidence to inherit.

## Required Reviewer Identity And Model

- Launch exactly one fresh direct Codex reviewer session with explicit model `gpt-5.6-sol`.
- The outer controller verified that local Codex model metadata lists slug `gpt-5.6-sol` with display name `GPT-5.6-Sol` before creating this handoff.
- If the explicit model cannot start, stop and report model unavailability. Do not silently fall back to another model or launch a replacement.
- The reviewer must not be any prior worker, reviewer, or controller identity, including:
  - `019f506f-9596-7892-a551-b0481cc95760`
  - `019f4fbc-a589-7183-aaa6-6cae506f9c36`
  - `019f50aa-0526-79a1-8fcb-13ee4537921a`
  - `019f50d9-f708-7ba1-b983-37e0e8bf740e`
  - outer controller `019f4a4a-3c0c-7212-9ef9-e28e72e857ab`
- Never use `ea-3`.
- Launch no helper, subagent, collaborator, fork, replacement, or second reviewer.
- The reviewer must report its actual fresh CLI session ID and configured model.

## Sandbox And Write Contract

Use `--dangerously-bypass-approvals-and-sandbox` from the first turn because the host read-only sandbox is proven unable to start commands with `bwrap: loopback: Failed RTM_NEWADDR`.

Bypass changes only host command viability. The reviewer remains contractually read-only over every repository and external system. The only permitted output is the outer wrapper's capture of the final response at:

`/tmp/GOVERNED_SPATIAL_RENDER_REVISION_7_CLEAN_INDEPENDENT_REREVIEW.final.md`

The reviewer must create no repository file and no `/tmp` harness, script, fixture, key, cache, bytecode, report, intermediate receipt, or generated artifact. Use `PYTHONDONTWRITEBYTECODE=1`. All Python must be supplied through standard input. Local Node may be invoked only with `node -e` and no script file.

## Bound Revision Evidence

Require these exact bytes before substantive review:

| Evidence | Exact path | Required SHA-256 | Mode |
| --- | --- | --- | ---: |
| Revision 6 schema | `/docker/chummercomplete/chummer-design/products/chummer/GOVERNED_SPATIAL_RENDER_CAPABILITY_QUOTA_EVIDENCE.schema.yaml` | `f86e6f737ba3333f7e84d8196481cda4c4cc34dc08d6b5aff5a7465af303546f` | `0664` |
| Revision 6 packet | `/docker/chummercomplete/chummer-design/products/chummer/review/GOVERNED_SPATIAL_RENDER_CANONICAL_AMENDMENT_PACKET.md` | `874f3ce32c160d396814381cee98ad936cb53bbb15f95a5591fecf9af17f82e7` | `0664` |
| Revision 6 assertion-correction handoff | `/docker/EA/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_6_ASSERTION_CORRECTION_HANDOFF.md` | `10f8df8d40e35c1938995e804f6716fcddd6c82a022976789d38fbce7090c024` | `0664` |
| Revision 6 controller receipt | `/tmp/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_6_CONTROLLER.final.md` | `50b31e2e064da1668f893892e2d1479950dab5b55134dd099496f90be1ce56ff` | `0600` |
| Revision 6 independent-review handoff | `/docker/EA/GOVERNED_SPATIAL_RENDER_REVISION_6_INDEPENDENT_REREVIEW_HANDOFF.md` | `6f9ba7d56f2ecffd26ee91794aa92caa6227f5d9c3943dd4af019149efeda361` | `0664` |
| Failed Revision 6 reviewer receipt | `/tmp/GOVERNED_SPATIAL_RENDER_REVISION_6_INDEPENDENT_REREVIEW.final.md` | `ab60ee5e8f3e2f7706dd57fb04520146125d5f28d0950bd2ada9abcf14735ae4` | `0600` |

The failed reviewer receipt is process evidence only. Its targeted spot checks do not satisfy or replace any Revision 7 matrix case.

## Exact Front-Door Paths

Read only these exact front-door files before the canonical corpus. Do not discover or enumerate alternatives.

| Exact path | Required SHA-256 |
| --- | --- |
| `/docker/EA/AGENTS.md` | `6a9b537f8ffa8d211898f61e36fd3bab41efe4b6facc5b5e1a1ca6524f8103e3` |
| `/docker/EA/.codex-design/product/README.md` | `c8994d6b5c0a398c5369da67a3670f3a178dc7866da56b636d00a1e0890e211e` |
| `/docker/EA/.codex-design/repo/IMPLEMENTATION_SCOPE.md` | `81235c00d6c0ac4b69978f4d0f192c6e29e1dd52eb72096b52cbac6caab77a6a` |
| `/docker/EA/.codex-design/review/REVIEW_CONTEXT.md` | `6cebbfeb7e5ee59f08dd087aaf27cd598680ef4324e7895ee2eb6cd5fce38520` |
| `/docker/chummercomplete/chummer-design/AGENTS.md` | `d03e34c4e19b4ba02b988ed54e126e20f6ee6834615c37385807be2a34da9f29` |

The outer controller attempted the required vexp front door and found `run_pipeline` unavailable. The reviewer must record that supplied preflight once. It must not probe MCP resources, list tools, search the web, or substitute a discovery mechanism.

## Exact Canonical Corpus: 18 Files

Read every byte of exactly these 18 canonical files. The first 17 are the packet manifest; the packet is the eighteenth. No repository enumeration is needed or permitted.

| # | Exact path | Required SHA-256 | Mode |
| ---: | --- | --- | ---: |
| 1 | `/docker/chummercomplete/chummer-design/products/chummer/CONTRACT_SETS.yaml` | `23155dabe089893345578905abfe8071866571c256b43dbc0dfef51edf534bc7` | `0664` |
| 2 | `/docker/chummercomplete/chummer-design/products/chummer/ARCHITECTURE.md` | `5d589a0c8957decf0a096c08f793dd94b58930917d093ae64bdbfa1a3b00021a` | `0664` |
| 3 | `/docker/chummercomplete/chummer-design/products/chummer/OWNERSHIP_MATRIX.md` | `43c13371696dbb62d747b4c368dfaf9cf5c95b1260f86a789aa26ce35f2c6334` | `0664` |
| 4 | `/docker/chummercomplete/chummer-design/products/chummer/PROGRAM_MILESTONES.yaml` | `75fc9f755d7f787b04b0e96083330d2651cb1c364736d39119d0a924da5755c0` | `0664` |
| 5 | `/docker/chummercomplete/chummer-design/products/chummer/HORIZON_REGISTRY.yaml` | `e3fff38375db7c77f46745395a9bea157e5867ffbdc970afc9065c34b530c6a2` | `0664` |
| 6 | `/docker/chummercomplete/chummer-design/products/chummer/MEDIA_ARTIFACT_RECIPE_REGISTRY.yaml` | `2114f6cab46618266b09541538ca41681fa1d893a4147cd246e4ebd81fd6bd03` | `0664` |
| 7 | `/docker/chummercomplete/chummer-design/products/chummer/projects/executive-assistant.md` | `ac954e62e167e7103bba78cefb76881b2f70922194bb2b244af42f0386d7d7af` | `0664` |
| 8 | `/docker/chummercomplete/chummer-design/products/chummer/projects/hub.md` | `1c1b3478edb092f9f8b02e3d8548eb53981ed219ce200d3c2743e4231b788854` | `0664` |
| 9 | `/docker/chummercomplete/chummer-design/products/chummer/projects/media-factory.md` | `6a386da7c76543dd3db8847f945a134c484a267d7590262b7c161b62303f9682` | `0664` |
| 10 | `/docker/chummercomplete/chummer-design/products/chummer/horizons/runsite.md` | `1de4a04b5efa6f07cb6d2c60bf342af51015a98096e6ccb1f61e55da43a850bb` | `0644` |
| 11 | `/docker/chummercomplete/chummer-design/products/chummer/sync/sync-manifest.yaml` | `014ff46ddce084f978b30423a0d054055f0d73a7e1a7e365c627730dbd94b58d` | `0644` |
| 12 | `/docker/chummercomplete/chummer-design/products/chummer/review/hub.AGENTS.template.md` | `75a532aabdb7a3fb4e9041bb909ba376e5a3180f6e01d6ddc4b10595ed9a9f82` | `0664` |
| 13 | `/docker/chummercomplete/chummer-design/products/chummer/review/media-factory.AGENTS.template.md` | `5f697fcc102f663946d94b2a3412aae8513b6964db9fe6632ef00ea7f613c003` | `0664` |
| 14 | `/docker/chummercomplete/chummer-design/products/chummer/review/hub-registry.AGENTS.template.md` | `7887ae72263dee617adbab09cf34a21a097157ae8a355f1969a5b869e8aac641` | `0644` |
| 15 | `/docker/chummercomplete/chummer-design/products/chummer/review/executive-assistant.AGENTS.template.md` | `6da44663bf2b3ee864107f236f9571680a452683faf97d61a48b6c9befcf1331` | `0664` |
| 16 | `/docker/chummercomplete/chummer-design/products/chummer/GOVERNED_SPATIAL_RENDER_PRIVACY_RETENTION_POLICY.md` | `028c221ebeca28a8702d15ade157bf2ce586322ed8d7581b041c31336caccfed` | `0664` |
| 17 | `/docker/chummercomplete/chummer-design/products/chummer/GOVERNED_SPATIAL_RENDER_CAPABILITY_QUOTA_EVIDENCE.schema.yaml` | `f86e6f737ba3333f7e84d8196481cda4c4cc34dc08d6b5aff5a7465af303546f` | `0664` |
| 18 | `/docker/chummercomplete/chummer-design/products/chummer/review/GOVERNED_SPATIAL_RENDER_CANONICAL_AMENDMENT_PACKET.md` | `874f3ce32c160d396814381cee98ad936cb53bbb15f95a5591fecf9af17f82e7` | `0664` |

Recompute the packet's embedded 17-row manifest independently and require `17/17`. Do not merely compare this table with the packet table; hash every path directly.

## Exact Governing-Evidence Corpus: 11 Files

Read every byte and recompute every hash for exactly these 11 governing inputs. No search or enumeration is needed or permitted.

| # | Exact path | Required SHA-256 | Mode |
| ---: | --- | --- | ---: |
| 1 | `/docker/chummercomplete/chummer-design/products/chummer/review/GOVERNED_SPATIAL_RENDER_PETITION_DECISION.md` | `2a5e4888bf2e9074a93e97e83d682e385eff53dd9c5ef8961fdc2fec6c2d1d6c` | `0600` |
| 2 | `/docker/EA/EA_GOVERNED_SPATIAL_RENDER_DESIGN_PETITION.md` | `ed4f8452d59760e11b6ab7784c9a35d272db4d62520d6c742740573424b3f45e` | `0644` |
| 3 | `/docker/EA/PROPERTYQUARRY_CHUMMER_GOVERNED_SPATIAL_RENDER_HANDOFF.md` | `e6ceebaedf91ef50a9e6179ac8775bbdb684147ffe1ca3ccc72175abcf68ee06` | `0600` |
| 4 | `/docker/EA/_completion/governed-spatial-render/GOVERNED_SPATIAL_RENDER_DESIGN_REVIEW_RECEIPT.generated.json` | `3226895f1946d519bf5be62e9795b81bd3985de383f8efa6113c4fa4a05deb2c` | `0600` |
| 5 | `/docker/property/PROPERTYQUARRY_GOVERNED_SPATIAL_RENDER_AUTHORITY_DECISION.md` | `401fe42211e2d8283ea9ca2a7cfc1a1eaffc80ff13c63fdf9e6158a116eff50a` | `0600` |
| 6 | `/tmp/GOVERNED_SPATIAL_RENDER_REVISION_2_INDEPENDENT_REREVIEW.final.md` | `be2cf8b882ae2652fd5e81d22512e20731629f2ac81f80f20c0ba1d494856979` | `0600` |
| 7 | `/docker/EA/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_3_HANDOFF.md` | `52f77bc7db4cf28b552523deefdabf92b5aabd9244914a04d2e8e1d0d733c5aa` | `0664` |
| 8 | `/docker/EA/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_4_RECOVERY_HANDOFF.md` | `4524fe394454911f58bfda153155d8945f200d5485ad2f1b0afcfdc32cafc0bb` | `0664` |
| 9 | `/tmp/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_4_WORKER.final.md` | `f0453ede69aec19f315442a719e949cae9e1badc9477ef2810c014bedf5c67ce` | `0600` |
| 10 | `/docker/EA/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_5_RECOVERY_HANDOFF.md` | `d84bb803522d921eb1b7fd81154cf0a5a8611f1d776f77f13f45056eab347704` | `0664` |
| 11 | `/docker/EA/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_6_ASSERTION_CORRECTION_HANDOFF.md` | `10f8df8d40e35c1938995e804f6716fcddd6c82a022976789d38fbce7090c024` | `0664` |

Recompute the packet's embedded governing-evidence table and require `11/11`. Hash every exact path directly.

## Absolute Discovery Prohibition

The reviewer has every allowed path above. It must not use or invoke:

- web search, URL open, or any network tool;
- MCP resource listing, MCP resource reading, tool discovery, or resource probing;
- `rg`, `grep`, `find`, filesystem globbing, wildcard expansion, or recursive search;
- `ls` or directory enumeration to discover files;
- broad `cat`, `sed`, `awk`, or Python traversal over a directory;
- `/proc`, shell history, Codex session storage, controller transcript, reviewer transcript, or terminal transcript access;
- any R5/R6 harness command, extracted command string, prior harness source, or prior harness implementation.

Targeted reads of the exact listed paths are permitted. Targeted `nl -ba <exact-path> | sed -n <range>` is permitted for line references. Git fingerprint commands against the five exact repository roots are permitted.

## Independence Order

1. Verify this handoff hash supplied by the outer controller and every front-door hash.
2. Verify the six bound Revision 6 evidence hashes/modes.
3. Read the governing petition decision and PropertyQuarry authority decision.
4. Read all 18 canonical files and all 11 governing inputs in full through exact paths.
5. Record candidate P0/P1/P2 defects and write an independent matrix plan in reasoning before reading the R6 controller receipt or failed R6 reviewer receipt.
6. Read the R6 controller receipt and failed reviewer receipt only after the independent candidate-defect pass.
7. Author the matrix from the canonical schema, packet, standards, and governing decisions. Do not derive it from controller or reviewer counts.
8. Execute the complete matrix through standard input.
9. Recompute hashes, modes, validators, and repository fingerprints after the matrix.
10. Return the final receipt and stop.

## Harness Retry Contract

Up to three total inline no-file harness executions are permitted only for harness-construction errors such as syntax, quoting, or an independently authored fixture baseline mistake.

- Disclose every run, exit code, construction error, and correction.
- Do not hide or relabel failed runs.
- Do not weaken a fixture, expected rejection layer, semantic rule, count, or acceptance criterion to obtain green.
- A genuine schema/contract failure is review evidence, not a construction error. Stop the matrix and return `REVISE` after a minimal in-memory confirmation if needed.
- `ACCEPT` is forbidden unless one complete independently authored execution covers every required group and all required cases pass.
- Exhausting three attempts without one complete run requires `REVISE`.

## Full Required Matrix

The fresh matrix must execute and report named cases, intended validation layer, pass/fail, and exact group tallies. Spot checks, prose inspection, or controller-count replay are insufficient.

### YAML And Schema

- duplicate-safe parse of all six canonical YAML files: `CONTRACT_SETS.yaml`, `PROGRAM_MILESTONES.yaml`, `HORIZON_REGISTRY.yaml`, `MEDIA_ARTIFACT_RECIPE_REGISTRY.yaml`, `sync-manifest.yaml`, and the capability/quota schema;
- reject duplicate YAML keys;
- `Draft202012Validator.check_schema`;
- attach and use `FormatChecker` on receipt fixtures.

### Duplicate-Safe Raw JSON

Positive coverage must include a simple object, safe-integer boundaries, and non-BMP Unicode.

Negative coverage must include:

- duplicate top-level and nested names;
- UTF-8 BOM;
- malformed UTF-8;
- unpaired high and low surrogates;
- `NaN` and `Infinity`;
- decimal float `1.0`;
- exponent float `1e0`;
- negative-zero token `-0`;
- integers above and below `-9007199254740991..9007199254740991`;
- trailing data;
- non-object root.

All reject before schema validation or signing.

### Bounded RFC 8785 And Node Parity

Independently implement the supported no-float domain:

- recursively reject floats, non-finite values, unsafe integers, unsupported types, non-string keys, and unpaired surrogates;
- check booleans before integers;
- sort object keys by UTF-16 code units using a language-equivalent to `key.encode('utf-16-be')`, never locale or Python code-point order;
- compactly serialize valid string/safe-integer/boolean/null scalars without ASCII escaping;
- verify the exact UTF-16 ordering vector containing carriage return, `1`, U+0080, U+00F6, U+20AC, a non-BMP key, and U+FB33;
- verify quote, reverse-solidus, control, newline, non-ASCII, nested array/object, boolean/null, and safe-boundary vectors;
- compare at least five independent supported-domain vectors byte-for-byte with local Node `JSON.stringify` after independent UTF-16 key sorting;
- reject invalid-domain programmatic floats, negative-zero float, NaN, infinity, unsafe integer, and invalid surrogate.

Do not claim general floating-point JCS behavior.

### Real Ed25519 And Signed Envelope

Use installed `cryptography` and a deterministic 32-byte seed.

- derive the exact raw 32-byte public key and SHA-256 fingerprint;
- start from the complete duplicate-safe parsed receipt;
- deep-copy it and delete exactly `signature.signature_value` and `signature.signed_payload_digest`;
- prove every selector and every other receipt member remains in signed bytes;
- bounded-JCS canonicalize, SHA-256 digest, sign those same bytes, and verify real Ed25519;
- require unpadded base64url text of exactly 86 characters and exactly 64 decoded bytes;
- reject non-canonical terminal bits and padding;
- positive deterministic ASCII and non-ASCII signed receipts;
- structural negatives for absent signature, absent/empty/wrong-type/short/long/padded/malformed/wrong-terminal signature value, `algorithm: none`, wrong algorithm, encoding, canonicalization, scope, empty key ref, negative epoch, malformed fingerprint/digest, and extra signature members;
- mutate algorithm, encoding, key ref, key epoch, fingerprint, canonicalization, scope, issuer, environment, expiry, and payload receipt ID without resigning and require failure;
- semantic/cryptographic rejection for unknown key ref/epoch, fingerprint mismatch, issuer mismatch, environment mismatch, revoked/not-yet-valid/expired key, receipt beyond key expiry, wrong digest, wrong Ed25519 signature, and payload tamper.

### Key Registry And Chronology

Require exact issuer/environment/key-ref/epoch/fingerprint identity, raw-key fingerprint binding, globally unique fingerprints, global revocation, no alias reactivation, no epoch reuse, and no epoch regression.

Independently test active alias duplicate, cross-environment fingerprint duplicate, global revocation alias, tuple/epoch reuse, reactivation, epoch regression, unknown key, fingerprint mismatch, and exact zero-skew validity boundaries.

Require:

`key.not_before <= receipt.issued_at <= receipt.expires_at <= key.not_after`

### Lifecycle, Lineage, Authorization, And Evidence

Execute coherent positive fixtures for all 11 build states:

1. `authorization_verified`
2. `reservation_held`
3. `released`
4. `attempt_committed`
5. `charge_pending`
6. `cancelled_reconciliation_pending`
7. `consumed`
8. `closed_consumed`
9. `compensation_pending`
10. `compensated`
11. `compensation_failed_blocked`

Required adversaries:

- five idempotency/lineage digest nulls times 11 states: `55/55` rejected;
- five original-authorization null/zero mutations times 11 states: `55/55` rejected;
- coherent expired, revoked, and blocked terminal positives with complete lineage;
- `compensation_failed_blocked` requires blocked posture/readiness plus blocked or engaged kill switch and complete reservation/attempt/mutation/consumption/compensation lineage;
- at least ten independent compensation-failed lineage-loss negatives;
- generic pre-execution blocked positive;
- generic blocked structural smuggling negatives `8/8` and semantic smuggling negatives `8/8` for individual digests, all digests, reservation, attempt/mutation, and consumption/compensation;
- audit-only positive with exact `not_present_audit_only`, all-null/zero authorization shape, zero-burn compose, and no execution lineage;
- globally coherent `valid` authorization fields and positive attempt maximum;
- exact stage-specific evidence families, including provider capability, exact compose-validator proof, current quota snapshot, and kill-switch refs where required;
- authorization binding over exactly owner, authorization ref, issue/expiry times, maximum attempts, and quota-limit digest;
- changed binding, scope, key, normalized request, composition, same-key conflict, concurrent duplicate, retry under different authorization, compensation under different authorization, duplicate compensation, optimistic refund, and attempt above immutable maximum rejection;
- reversed/naive timestamp, capability-window, evidence-expiry, compose-freshness, quota/kill-switch freshness, authorization age, reservation lease, key validity, and canary duration/age negatives using offset-aware RFC 3339 arithmetic.

A negative counts only when the intended validator rejects the intended defect and unrelated fields remain valid.

### Cross-File Assertions: 18/18

Execute all 18 ownership, recipe, RUNSITE, privacy, milestone, mirror, and PropertyQuarry assertions. Include explicitly:

1. `governed_spatial_render_v1` registered with media-factory owner.
2. PropertyQuarry bridge owner is exactly `/docker/property` / `app.product` / `app.product.property_tour_hosting`.
3. Hub bridge and media-factory quota/execution boundaries.
4. EA provider-redacted derived-only and zero-burn assistance boundary.
5. PropertyQuarry privacy owner `app.api.routes.landing`, minimization dependency `public_tour_payloads`, and deletion executor `property_tour_hosting`.
6. `PROGRAM_MILESTONES.yaml` lowercase contains literal `spatial-render` and `blocked`.
7. Both separate Chummer recipes exist and remain isolated.
8. RUNSITE orientation remains public/non-combat; encounter preview remains private fictional non-graphic and consumes immutable mechanics/outcome refs without calculating rules.
9. Public and PropertyQuarry consumers reject combat overlays.
10. `sync-manifest.yaml` contains the capability/quota schema and privacy/retention policy filenames and does not require the amendment-packet path.
11. Numeric privacy/retention and exact external owners remain coherent.
12. All four review templates bind the governed boundary.
13. Continuous all-walkable-room coverage has no exclusion escape and retains no-cut/no-teleport/collision/continuity/effective-motion gates.
14. Provider capability and artifact readiness remain separate, fail closed, and provider-redacted.

Report all 18 named assertions individually.

### Hashes, Validators, Boundaries, And Sync

- independently hash packet manifest `17/17`;
- independently hash governing evidence `11/11`;
- verify all six bound Revision artifacts and five front-door hashes/modes;
- `python3 scripts/ai/validate_contract_sets.py`: exit `0`, output `ok`;
- `git diff --check`: exit `0`;
- `python3 scripts/ai/validate_sync_manifest.py`: known baseline only, exit `1`, exactly 8 missing sources, 56 mirror expansions, 64 total diagnostics, zero governed-spatial diagnostics;
- stale PropertyQuarry unresolved-authority contradictions `0` across the 18 files;
- literal canonical `ea.*` aliases within `governed_spatial_render_v1` `0`;
- schema and packet mode `0664`;
- exact protected-repository pre/post fingerprints and reviewer write count `0`.

## Ownership And Claim-Ceiling Review

Confirm independently:

- Chummer design owns canon;
- media-factory owns contract/execution receipts, provider adapters/jobs, immutable output manifests, lifecycle/deletion, idempotency, and quota mutation;
- Hub owns Chummer bridge, approved refs, purpose, permissions, audience, authorization, provider-redacted product meaning, and closeout;
- Registry owns publication leases, renewal, revocation, public refs, cache withdrawal, and tombstones;
- Fleet owns execution-budget, gate, canary, rollback, and landing evidence;
- EA owns no Chummer contract, product, provider, quota, publication, or readiness truth;
- PropertyQuarry owners remain external and exact, and implementation remains blocked pending its numeric policy and independent review;
- style packs remain provider-neutral, data-driven, and rights/provenance-bound;
- design references are not licenses;
- accepted current exact-family capability receipts remain `0`;
- legal projection remains `unverified_or_blocked`;
- no design, receipt, environment variable, historical mention, compose result, or hosted link proves provider capability, artifact readiness, implementation, launch, or flagship readiness.

## Repository Fingerprint Contract

The outer controller supplies launch-pre fingerprints after this handoff is created. Recompute at review end for exactly:

- `/docker/EA`
- `/docker/chummercomplete/chummer-design`
- `/docker/property`
- `/docker/chummercomplete/chummer.run-services`
- `/docker/chummercomplete/chummer-hub-registry`

Use ordered tuple:

`HEAD / SHA256(git diff --raw -z) / SHA256(git diff --cached --raw -z) / SHA256(git status --porcelain=v2 -z)`

Chummer, PropertyQuarry, run-services, and hub-registry must remain exact. EA may exhibit concurrent external drift; report it honestly and prove reviewer writes `0` from direct actions and bound-file hashes. Never claim EA unchanged when it drifted.

## Decision Rules

Return `ACCEPT` only when:

- one complete independently authored matrix execution covers every required group;
- every required positive and negative case behaves at its intended validation layer;
- no P0 or P1 content or process finding remains;
- all 18 canonical files and 11 governing inputs were read and independently hash-verified;
- all owners, privacy rules, provider boundaries, cryptographic semantics, lineage rules, state transitions, and claim ceilings are coherent;
- repository/action-log parity proves zero reviewer writes and zero prohibited actions.

Return `REVISE` for any content defect, material ambiguity, incomplete matrix, exhausted harness retries, invalid receipt acceptance, lineage loss, signature or key-registry bypass, unsafe numeric path, authority leak, hash/mode mismatch, prohibited action, or inability to prove required coverage. P2 observations may accompany `ACCEPT` only when clearly non-blocking.

The reviewer cannot authorize implementation. `ACCEPT` means only that the outer controller may consider a separate bounded implementation handoff after independently auditing the receipt.

## Forbidden Actions

Required count `0`:

- repository edits, repairs, mode changes, or generated files;
- controller/reviewer transcript access or prior harness implementation access;
- web, MCP probing, network, search, enumeration, or broad filesystem operations;
- helper, subagent, collaborator, fork, replacement, second reviewer, or `ea-3` launch;
- package installation, tool update, implementation, runtime, route, adapter, API, provider, or product mutation;
- provider/account/balance/credential/quota calls;
- upload, provider job, reservation, consumption, cancellation, compensation, build, or test build;
- browser, video, tour, FPS, accessibility, or canary action;
- deployment, mirror/public publication, promotion, or readiness projection;
- PropertyQuarry mutation;
- Telegram or any other notification.

Telegram message `3774` is historical. The reviewer sends none.

## Final Receipt

The wrapper-captured final response at `/tmp/GOVERNED_SPATIAL_RENDER_REVISION_7_CLEAN_INDEPENDENT_REREVIEW.final.md` must contain:

- decision exactly `ACCEPT` or `REVISE` as the final line;
- actual fresh session ID and exact model `gpt-5.6-sol`;
- findings first, ordered P0, P1, P2, with exact file and line references and explicit `none` for empty classes;
- exact hash/mode table for this handoff, all six bound Revision artifacts, five front-door files, 18 canonical files, and 11 governing inputs;
- confirmation all exact paths were read without discovery or enumeration;
- packet manifest `17/17` and governing evidence `11/11` with direct hashes;
- every harness execution attempt, construction error, correction, exit code, and final complete-run status;
- full independently authored matrix with named groups, cases, intended layer, and exact tallies;
- explicit bounded RFC 8785 and Node parity verdict;
- explicit real deterministic Ed25519, exact two-member deletion, canonical terminal-bit, envelope-mutation, key-registry, global revocation, and chronology verdict;
- explicit all-11-state, lineage, authorization, generic-blocked, compensation-failed, audit-only, evidence-family, attempt-limit, and freshness verdicts;
- all 18 named cross-file assertions, including the two corrected predicates;
- validator, sync-baseline, mode, stale-authority, and alias results;
- exact launch-pre/post repository fingerprints and reviewer action counters;
- explicit statement that no controller/reviewer transcript, R5/R6 command, or prior harness implementation was accessed or reused;
- explicit zero counts for web, MCP probing, network, search, enumeration, writes, helpers, live actions, and Telegram;
- remaining implementation, PropertyQuarry policy, provider evidence, runtime journey, canary, publication, promotion, and readiness gates;
- explicit statement that the decision is not implementation authorization and not a launch/readiness claim.

Stop after the final receipt. Do not implement, authorize, notify, or launch another session.
