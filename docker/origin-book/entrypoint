#!/bin/sh
set -eu
# Private X socket in this container's tmpfs, never the host desktop/X server.
mkdir -p /tmp/.X11-unix
chmod 1777 /tmp/.X11-unix
Xvfb :97 -screen 0 1280x900x24 -nolisten tcp -ac &
exec python -m scripts.origin_browser_container "$@"
