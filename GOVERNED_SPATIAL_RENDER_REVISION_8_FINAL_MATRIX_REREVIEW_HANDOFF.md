# Governed Spatial Render Revision 8 Final-Matrix Re-Review Handoff

Date: 2026-07-11 (Europe/Vienna)

State: `authorized_for_one_same_session_final_matrix_rereview`

Decision ceiling before this review: `REVISE`

Implementation state before this review: `blocked`

## 1. Purpose

Revision 8 exists only to close or confirm the single Revision 7 process finding:
the independent reviewer did not obtain one standalone complete green `341/341`
matrix execution within the Revision 7 three-attempt ceiling.

Revision 8 authorizes exactly one continuation attempt by the same independent
reviewer identity, using that reviewer's own independently authored in-memory
harness with every correction disclosed in the Revision 7 receipt integrated
before execution.

This handoff does not authorize implementation, provider execution, account or
quota activity, deployment, publication, promotion, launch, or readiness claims.
Even an `ACCEPT` decision closes only this independent design-review gate; no
implementation or live action may occur in this turn.

## 2. Exact bound artifacts

The outer controller independently verified these bytes before creating this
handoff. Any mismatch is an immediate fail-closed `REVISE` result.

| Artifact | Required SHA-256 | Mode |
| --- | --- | ---: |
| `/docker/chummercomplete/chummer-design/products/chummer/GOVERNED_SPATIAL_RENDER_CAPABILITY_QUOTA_EVIDENCE.schema.yaml` | `f86e6f737ba3333f7e84d8196481cda4c4cc34dc08d6b5aff5a7465af303546f` | `0664` |
| `/docker/chummercomplete/chummer-design/products/chummer/review/GOVERNED_SPATIAL_RENDER_CANONICAL_AMENDMENT_PACKET.md` | `874f3ce32c160d396814381cee98ad936cb53bbb15f95a5591fecf9af17f82e7` | `0664` |
| `/docker/EA/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_6_ASSERTION_CORRECTION_HANDOFF.md` | `10f8df8d40e35c1938995e804f6716fcddd6c82a022976789d38fbce7090c024` | `0664` |
| `/tmp/GOVERNED_SPATIAL_RENDER_CANONICAL_REVISION_6_CONTROLLER.final.md` | `50b31e2e064da1668f893892e2d1479950dab5b55134dd099496f90be1ce56ff` | `0600` |
| `/docker/EA/GOVERNED_SPATIAL_RENDER_REVISION_7_CLEAN_INDEPENDENT_REREVIEW_HANDOFF.md` | `2bc5753208903ebc8e60d15793688c66bcdeadcff85ba0d6c8eea0e84c9c096e` | `0664` |
| `/tmp/GOVERNED_SPATIAL_RENDER_REVISION_7_CLEAN_INDEPENDENT_REREVIEW.final.md` | `7f92d24a54899ea951403a2d9c9609b7bc05251b9f8cb0400a1b1937b94cd959` | `0600` |

The schema and packet bytes are frozen. Revision 8 authorizes no canonical-file
change and no repository write.

## 3. Reviewer identity and separation of duties

Resume exactly this existing independent reviewer:

`019f50e9-f97f-7dc0-88e1-8e515e5e2ac3`

The exact model is:

`gpt-5.6-sol`

Required execution posture:

- resume that exact session ID and its live inherited context
- use exact model `gpt-5.6-sol`
- keep web disabled
- use bypass mode because this host's read-only sandbox is already proven broken
- do not launch a new reviewer, replacement, helper, subagent, collaborator,
  fork, controller, or `ea-3`
- do not silently fall back to another model or identity

Inherited live session context is permitted. Reading any transcript file or any
controller/reviewer transcript through a command is forbidden.

## 4. Narrow supersession

Revision 8 supersedes only the Revision 7 three-attempt ceiling.

It does not supersede or weaken any other Revision 7 requirement, authority
boundary, evidence binding, denominator, fixture, semantic expectation,
forbidden action, separation-of-duties rule, fail-closed condition, or claim
ceiling.

The replacement ceiling is exactly one continuation attempt. There is no retry,
repair run, correction-only run, subset run, or follow-up spot check if that
attempt fails to complete at exactly `341/341`.

## 5. Authorized reviewer action

The reviewer may perform only these actions:

1. Construct, entirely in the command channel and without a persistent file,
   one complete version of its own independently authored Revision 7 harness.
2. Integrate every correction disclosed in the Revision 7 receipt before the
   command is executed.
3. Execute the full 341-case matrix once through Python standard input with
   `PYTHONDONTWRITEBYTECODE=1`.
4. Use local `node -e` only where the already-required supported-domain Node
   parity cases invoke it from that one harness.
5. Run the exact post-review repository parity check after the harness, only if
   the harness command itself completed.
6. Return one final receipt body ending in exactly `ACCEPT` or `REVISE`.

There may be no preliminary command, syntax probe, fixture probe, partial run,
correction confirmation, denominator inspection, or second harness command.

No standalone file-read command is authorized. The single matrix process may
consume only the exact already-bound canonical/governing paths that its existing
341 cases require, without printing or interactively inspecting their contents.
The exact post-parity command may read only the five named repository states.
These required in-process inputs do not authorize any additional file read,
discovery, or exploration.

## 6. Mandatory integrated Revision 7 corrections

The one complete harness must integrate all Revision 7 construction corrections
before execution, including all of the following:

1. Raw JSON surrogate rejection traverses the decoded object recursively, so
   escaped unpaired high and low surrogates are rejected after JSON decoding.
2. The RFC 8785 supported-domain ordering vector compares actual key ordering by
   UTF-16 code units and does not parse escaped serialized keys incorrectly.
3. The positive generic `blocked` fixture uses the exact valid pre-execution
   authorization and blocked-posture shape; it does not inherit the audit-only
   authorization shape.
4. The EA cross-file assertion uses the canonical derived-only, provider-redacted,
   synthetic zero-burn compose-assistance boundary.
5. The combat-overlay cross-file assertion uses the canonical public and
   PropertyQuarry combat-exclusion boundaries rather than a narrow accidental
   filename or sentence spelling.
6. The four review-template assertions bind their actual governed-spatial
   contract and owner boundaries rather than one narrow prose spelling.
7. `same_key_conflict` maps to `same_key_different_digest` plus
   `reject_conflict`.
8. `concurrent_duplicate` maps to
   `one_job_one_reservation_one_attempt`.
9. Retry and compensation under a different original authorization map to the
   exact immutable authorization-lineage semantic contract.
10. Capability/artifact separation maps to the exact rule that capability
    evidence and design intent never equal artifact readiness.

These corrections may repair only harness construction. They may not remove a
case, change an expected result, reduce a denominator, replace an adversarial
mutation with a prose-presence check, or turn a semantic verifier case into a
constant pass.

## 7. No placeholder evidence

Every case must execute the behavior named by its group.

The following are forbidden as evidence for an adversarial or operational case:

- `check(..., True)` or an equivalent constant pass
- counting a case without executing its mutation and expected rejection
- replacing a semantic-verifier fixture with a schema-description substring
- replacing key-registry state transitions with prose-presence checks
- replacing chronology arithmetic with a constant boolean
- replacing validator execution or repository parity with asserted constants
- carrying a pass forward from Revision 6 or Revision 7
- aggregating results across previous failed or partial runs

Textual checks remain appropriate only for the explicitly named cross-file canon
assertions, manifest rows, and exact hash/mode bindings.

## 8. Frozen complete matrix

The single run must print and satisfy every exact group below.

| Group | Required result |
| --- | ---: |
| `yaml_duplicate_safe` | `6/6` |
| `schema_meta` | `2/2` |
| `raw_json_positive` | `3/3` |
| `raw_json_negative` | `15/15` |
| `bounded_jcs` | `6/6` |
| `bounded_jcs_negative` | `6/6` |
| `node_parity` | `5/5` |
| `build_state_positive` | `11/11` |
| `ed25519_positive` | `2/2` |
| `signature_structural_negative` | `17/17` |
| `signed_envelope_mutation` | `11/11` |
| `signature_semantic_negative` | `12/12` |
| `key_registry_negative` | `10/10` |
| `idempotency_null_negative` | `55/55` |
| `authorization_lineage_negative` | `55/55` |
| `blocked_terminal_positive` | `6/6` |
| `compensation_lineage_loss_negative` | `10/10` |
| `generic_blocked_positive` | `1/1` |
| `generic_blocked_structural_negative` | `8/8` |
| `generic_blocked_semantic_negative` | `8/8` |
| `audit_only_positive` | `1/1` |
| `semantic_adversary_negative` | `11/11` |
| `chronology_freshness_negative` | `13/13` |
| `cross_file_assertion` | `18/18` |
| `manifest` | `18/18` |
| `governing_hashes` | `11/11` |
| `repository_validator` | `2/2` |
| `sync_baseline` | `5/5` |
| `boundary_scan` | `4/4` |
| `protected_repo_parity` | `5/5` |
| `owned_file_hash` | `4/4` |
| **Total** | **`341/341`** |

The manifest group remains 17 exact canonical hash rows plus one row-count
assertion. The governing group remains 10 exact packet-bound governing rows plus
one row-count assertion. The R8 handoff is an additional outer-controller binding
and does not alter either frozen denominator.

## 9. Technical matrix requirements

The complete run must independently execute, not merely mention:

- duplicate-safe YAML loading and Draft 2020-12 schema meta-validation with
  `FormatChecker`
- duplicate-safe raw JSON, strict UTF-8, BOM rejection, decoded-surrogate
  rejection, float/non-finite/negative-zero rejection, and safe-integer bounds
- the bounded no-float RFC 8785/JCS profile with UTF-16 key ordering, compact
  Unicode-preserving scalar serialization, escaping vectors, and local Node
  `JSON.stringify` parity for the supported domain
- deterministic real Ed25519, exact deep-copy deletion of only
  `signature_value` and `signed_payload_digest`, canonical 64-byte signature
  encoding, terminal bits, digest verification, and envelope mutations
- global fingerprint uniqueness, tuple resolution, revocation, no reactivation,
  no epoch reuse/regression, issuer/environment binding, and zero-skew key
  chronology
- every lifecycle positive and adversarial state mutation, including exact
  attempt limits, authorization coherence and immutable lineage,
  `compensation_failed_blocked`, audit-only, generic blocked structural `8/8`,
  and generic blocked semantic `8/8`
- actual timestamp parsing, ordering, freshness arithmetic, offset awareness,
  quota/kill-switch/authorization/reservation/canary windows, and key validity
- all 18 cross-file assertions, 17/17 manifest replay, 11/11 governing replay,
  validators, known sync baseline, boundary scans, modes, action counters, and
  protected-repository parity

## 10. Post-parity requirement

After the one matrix process exits, the reviewer may run one exact local
post-parity command over only these repositories:

- `/docker/EA`
- `/docker/chummercomplete/chummer-design`
- `/docker/property`
- `/docker/chummercomplete/chummer.run-services`
- `/docker/chummercomplete/chummer-hub-registry`

For each repository, record exactly:

1. `git rev-parse HEAD`
2. SHA-256 of `git diff --raw -z`
3. SHA-256 of `git diff --cached --raw -z`
4. SHA-256 of `git status --porcelain=v2 -z`

The outer controller supplies the post-handoff pre-review baselines in the resume
prompt. Chummer design, PropertyQuarry, run-services, and hub-registry must remain
exact. EA drift must be reported honestly and attributed only when the action log
proves attribution; concurrent external EA drift is not a reviewer write.

## 11. Reviewer-forbidden actions

Reviewer counts must remain zero for:

- standalone file reads or content inspection
- transcript reads or controller-harness reuse
- repository discovery, enumeration, `rg`, `grep`, `find`, globbing, or `ls`
- web, network, MCP, or resource probing
- helper, subagent, collaborator, fork, replacement, second reviewer, or `ea-3`
- repository or `/tmp` writes
- persistent harness, fixture, cache, bytecode, key, report, or generated file
- schema, packet, canon, handoff, or mode mutation
- PropertyQuarry, run-services, hub-registry, or other product mutation
- provider/account calls, quota reads or mutations, reservations, jobs, uploads,
  builds, cancellation, or compensation
- browser, video, tour, canary, deployment, publication, promotion, launch, or
  readiness action
- Telegram or any other notification

The outer CLI wrapper alone may write the exact final response to:

`/tmp/GOVERNED_SPATIAL_RENDER_REVISION_8_FINAL_MATRIX_REREVIEW.final.md`

That receipt must be mode `0600` and byte-for-byte equal to the final reviewer
response.

## 12. Required final receipt

The final reviewer response must include:

- exact session ID and exact configured model
- explicit acknowledgement that this was the same-session Revision 8
  continuation and not a replacement
- the R8 handoff hash supplied by the outer controller
- all six bound artifact hashes and modes
- exactly one matrix-attempt disclosure
- every group tally and total
- any failed case with reproducible evidence
- post-parity tuples for all five repositories
- action counters for every forbidden class
- P0, P1, and P2 findings
- the unchanged claim ceiling
- a final line containing exactly `ACCEPT` or `REVISE`

## 13. Decision rule

Return `ACCEPT` only if all of the following are true:

1. The exact resumed identity is
   `019f50e9-f97f-7dc0-88e1-8e515e5e2ac3`.
2. The exact configured model is `gpt-5.6-sol`.
3. Exactly one complete matrix process executed.
4. That one process printed every frozen group and exactly `341/341`.
5. No case was weakened, removed, constant-passed, or replaced by an invalid
   evidence type.
6. Post-parity checks are green for the protected repositories.
7. All bound hashes and required modes remain exact.
8. Reviewer-attributable writes and every forbidden-action count are zero.
9. P0, P1, and P2 findings are all `none`.

Return `REVISE` for any command-construction failure, incomplete execution,
non-`341/341` total, missing group, invalid fixture, denominator change,
placeholder evidence, parity mismatch, identity/model mismatch, prohibited
action, or substantive defect. There is no retry in Revision 8.

An `ACCEPT` decision is design-gate evidence only. It does not itself authorize
or prove implementation, provider capability, account or quota authority, build
behavior, key custody, artifact readiness, PropertyQuarry implementation,
canary, deployment, publication, promotion, launch, or flagship readiness.

## 14. Outer-controller closeout

After reviewer exit, the outer controller must independently verify:

- exact model and session identity from the session transcript metadata
- final receipt byte equality to the final transcript response
- receipt SHA-256 and mode `0600`
- R8 handoff and all six bound artifact hashes and modes
- exactly one complete matrix tool action and one permitted post-parity action
- no prohibited reviewer action
- protected-repository post-parity
- concurrent EA drift honestly

The outer controller then reports `ACCEPT` or `REVISE` and stops. It may not
implement, deploy, execute a provider, publish, promote, or claim readiness in
this turn.
