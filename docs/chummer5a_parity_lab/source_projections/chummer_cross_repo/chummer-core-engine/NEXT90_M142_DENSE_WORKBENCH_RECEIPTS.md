# M142 dense workbench receipt projection

- `workflow:initiative` -> `SessionActionBudgetDeterministicReceipt`
- `workflow:lifestyles` -> `WorkspaceWorkflowDeterministicReceipt`

The separate UI interaction receipt for `workflow:initiative` remains intentionally absent from the GM runboard projection so the packet continues to fail closed on that live gap.
